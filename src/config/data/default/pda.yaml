# @package data
pda:
  key_dir: ${hydra:runtime.config_sources\[1\].path}/../keys/train/PDBbind_v2020/pda
  data_dir:
  processed_data_dir:
  label_file:
  loss_ratio: 1.0
  objective: regression
  train_result_path: train_pda.txt
  test_result_path: test_pda.txt
  n_samples: 0
  pos_noise_std:
  pos_noise_max:
